# Hotel Butler 设计准则

本文件是 desktop 与 server 管理后台共用的产品体验基线，只在仓库根目录维护。具体颜色、字号和组件 token 以各应用现有样式与组件实现为准，不在这里重复生成一套主题。

## 产品界面

| 界面 | 使用者 | 核心目标 |
|---|---|---|
| desktop | 酒店一线运营人员 | 用尽可能少的干扰完成当前操作 |
| server 管理后台 | 业务管理人员 | 查找、比较、管理和追踪 desktop 用户及业务数据 |

两者共享品牌语言和基础组件，但不要求使用相同的信息架构。desktop 是任务工作台，server 是业务管理工具。

## 通用原则

- 先明确用户当前要完成的任务，再决定页面结构；不为了“显得完整”增加模块。
- 主操作清晰且数量克制，次要信息和高级操作使用渐进披露。
- 用层级、分组、间距和熟悉的控件表达关系，少用解释性文案和装饰元素。
- 必须提供清楚的 loading、empty、disabled、success、validation 和 error 状态。
- 保留必要的上下文、可访问性、错误恢复和危险操作确认，简洁不能以信息缺失为代价。
- 优先复用邻近页面的交互模式和已有组件，不为单个页面另造视觉体系。

## Desktop

- 默认只展示完成当前任务必需的信息；辅助说明、低频设置和高级操作按需展开。
- 避免把多个业务阶段、统计卡片、提醒和操作入口同时堆在首屏。
- 每个主要区域应有明确焦点；同一层级不要出现过多等权操作。
- 本地数据与 server 数据按业务边界处理。只有当同步状态会影响用户判断或恢复操作时，才在 UI 中强调来源和同步状态。
- 保持桌面工作台的操作效率，减少不必要的跳转、确认和重复输入。

## Server 管理后台

- 页面从管理人员的业务问题出发，例如“找到目标用户”“判断当前状态”“处理异常”“追踪操作结果”，而不是从数据库表结构出发。
- 列表、筛选和详情围绕实际决策字段组织；默认视图突出高频条件和异常状态。
- 需要比较的数据优先采用易扫描的表格或列表。图表只在能帮助判断趋势或异常时使用。
- 批量操作、权限操作和不可逆操作必须明确范围、结果与失败反馈，并保留必要的审计线索。
- 管理后台可以比 desktop 信息更密集，但必须通过分组、固定层级和渐进披露保持可读性。
- 不照搬面向消费者的营销页布局，也不使用无业务目的的指标卡或装饰性仪表盘。

## 实现约束

- 两端均优先使用现有 Svelte 5、shadcn-svelte、Tailwind CSS 和 Lucide 组件体系。
- 语义颜色和主题值放在各应用的全局样式中，组件内避免硬编码重复 token。
- 使用 Lucide 作为界面图标来源；图标按钮必须有可访问名称，陌生或重要操作保留文字标签。
- 动效应帮助理解状态和空间变化，并尊重 reduced-motion；不添加持续循环或纯装饰动效。
- desktop renderer 的特权操作必须经过 preload / IPC；server 页面不得绕过既定服务和权限边界直接组织业务逻辑。
