# Tasks

- [x] 1. Add the model-gateway port, purpose policies and LangChain adapter with focused tests.
- [x] 2. Inject the gateway into runtime, routing, summarization and title generation, removing
      direct provider construction from those modules.
- [x] 3. Update the server composition root and verify direct provider construction is centralized.
- [x] 4. Run targeted tests during implementation and the affected server completion gate once.
- [x] 5. Record independent verification and review evidence, then synchronize the stable runtime
      specification.
