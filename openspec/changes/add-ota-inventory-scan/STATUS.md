# 状态：OTA 价量态定时扫描（Change B）

**2026-09-21｜携程 + 美团双渠道实装，⭐ 立论场景（9.3）真机通过。13 次提交未推送。**

**下一步：剩余真机项 9.4~9.7 + `issues.md` 的两条。**

分支：`feature/ota-inventory-snapshot`（Change A 与 B 在同一分支）

---

## 已完成

| 节 | 内容 | 新增测试 |
|---|---|---|
| 1 | ✅ **连通性验证（真机通过）** | — |
| 2 | 失效判据抽取（携程内部复用） | 既有 24 项未改仍全绿 |
| 3 | 取数层 `ctrip/inventory-scan.ts` | 18 |
| 4 | 调度器 `inventory-scan-dispatcher.ts` | 22 |
| 5-6 | 比对上报 + payload 规格 + 服务端需求文档 | 10 |
| 7 | 配置三层开关 + `mergeConfig` 深度 | 5 |
| 8 | 装配（app-scope） | — |
| — | **code review 修复**（`f4d25bb`） | +13 |

**本 change 累计新增 85 项**（72 + review 修复的 13）；2026-09-21 实测全量
**1233 passed / 130 files，零失败**。typecheck 主进程干净；lint 4 个错全在本 change
未触及的 meituan 测试文件。

⚠️ 之前记的「1120 passed / 1 failed，那 1 个是 `__SERVER_ORIGIN__`」现已不复现 ——
该用例不在当前 unit 套件里，**未深究原因**，只如实记录当下观测。

### 新增文件

```
channels/ctrip/session-expiry.ts          失效判据（从 readback 抽出）
channels/ctrip/inventory-scan.ts          ⭐ 两步请求取数
channels/ctrip/inventory-scan-payload.ts  ⭐ 上报体规格（RMS 对接读这份）
channels/inventory-scan-dispatcher.ts     ⭐ 调度器（第六种触发模型）
inventory-snapshot/scan-to-report.ts      比对 → 上报
openspec/changes/add-ota-inventory-scan/服务端需求.md
```

---

## ✅ 连通性验证（2026-09-20，两个携程账号）

**结论：`session.fetch` 方案成立，不需要降级。** 这推翻了立项时设想的三层降级
（找已开标签 → 启隐藏浏览器 → 裸 HTTP）。

| 验证项 | 结果 |
|---|---|
| `credentials: 'include'` 对第三方域带 cookie | ✅ 返回真实数据，非登录页 |
| 携程认主进程发起的请求 | ✅ 200 + `application/json` |
| 需要补的头 | **只需 `Referer` + `Origin`**，无签名头 |
| 房型清单 | ✅ 31 / 13 个房型 |
| 取数（含价格） | ✅ `roomStatusResult` + `roomPriceResult` 都回来了 |
| 两 partition 互不串味 | ✅ 各返回自己酒店 |

**标签页开/关对照实验**（用户只开了云朵那个账号）：

```
第一轮 18:36  两个账号都没开标签页  → 31 / 13 房型
第二轮 18:38  只开了云朵            → 31 / 13 房型，bodyLength 逐字节相同
```

同一轮里开着的与没开的**表现一致** —— cookie jar 不受标签页状态影响。

**额外证实**：探针跑的两个时刻基线库**零写入**，同期 `page-read` 日志出现在手动开
标签页的时刻，与探针不重合 —— `session.fetch` 不经渲染进程，**不会被自己的 CDP 拦到**。

---

## ⚠️ 测试前必读

### 1. 总闸默认关闭

`inventoryScan.enabled` 默认 `false` —— 有外部副作用的周期性行为不该因装新版本就自己跑。
**测试前要先打开**，且 `idleMs` 默认 5 分钟，等一轮较久。

建议临时改 `app-config/defaults.ts` 的 `enabled: true`，验完撤回。

⚠️ **`idleMs` 所有环境统一 5 分钟**（`21bb119`，原 dev 档 60 秒已取消）。等一轮较久是
正常的；真要调快就临时改 `defaults.ts` 的 `SCAN_PACE`，验完撤回。

### 2. ⚠️ 正常情况下**绝大多数轮次应该零上报**（用户提出）

这不是 bug，是设计如此：

```
用户主动改 → 回读已经更新了基线 → 扫描比对时发现一致 → 不报   ← 绝大多数
有人在别处改 → 基线是旧的 → 扫描发现差异 → 上报              ← 立论场景，少见
```

**反过来说：如果每轮都在报，那才是有 bug。**

所以 9.3 那条验证（在**其他浏览器**改一次价）是唯一能证明链路有效的场景 —— 光看
"跑起来了没报错"说明不了任何问题。

### 3. 首轮只建基线不上报

库里没有基线的格子只写不报。所以**第一轮扫描必然零上报**，第二轮起才可能报。

---

## 剩余任务（全要真机）

| # | 内容 |
|---|---|
| ~~9.2~~ | ✅ **已通过**（2026-09-21 晚）—— 不开标签页，4 个目标全命中 |
| ~~9.3~~ | ✅ **已通过**（2026-09-21 晚）—— 立论场景，改房量与关房各验一次 |
| 9.4 | 无变化时连续两轮 → 第二轮零上报 ⚠️ 观察到多轮 `changed: 0`，但未严格构造对照 |
| 9.5 | 首轮全新窗口 → 只建基线零上报 |
| 9.6 | cookie 失效（手动清 partition cookie）→ 跳过该账号 + GlitchTip 有记录 |
| 9.7 | 关掉某家店的开关 → 该店不扫，同渠道其余店照常 |
| ~~9.8~~ | ✅ **已通过**（2026-09-21 晚）—— 一轮 4 秒，无风控迹象；dev 也统一成 5 分钟 |
| 10.1 | ⚠️ 回头处理 `issues.md` 的 ISSUE-1（读端点寄生在写适配器里） |
| 10.2 | `issues.md` 的 ISSUE-2（`isHotelEnabled` 无测试覆盖）—— 用户明确「后面再加」 |

### 查库命令

```bash
DB=~/Library/Application\ Support/小智酒店管家\[开发\]/hotel-butler.sqlite
sqlite3 -header -column "$DB" \
  "SELECT item_type, source_of_truth, COUNT(*) n FROM ota_inventory_snapshot GROUP BY 1,2;"
```

### 看日志

⚠️ **dev 构建的日志不落盘**，只打在启动应用的那个终端（`configure-main-logging.ts`：
`isPackaged` 为 false 时 console 走 `debug`，file transport 的目录不会被创建）。
2026-09-21 实测 `~/Library/Logs/小智酒店管家[开发]/` **不存在**。

打包版（预发/正式）才有文件：

```bash
grep -a "Inventory scan\|Snapshot cells flushed" \
  ~/Library/Logs/小智酒店管家\[预发\]/staff/main.log | tail -20
```

⚠️ 路径里是 `小智酒店管家[环境]`（`app.setName(APP_PRODUCT_NAME)`），
**不是 `Electron`** —— 原文记的路径是错的。

---

## 2026-09-21 code review 修复（`f4d25bb`）

两遍独立 review（后台 agent + 主上下文）合并去重后 8 条，修了 6 条。

### 删除：静默窗口整套

`lastWriteAt` 恒为 `null`，`quietAfterWriteMs` / `isWithinQuietWindow` / 3 条测试全是
死代码。**用户判定：比时间差不是冲突的真实判据**（60 秒是猜的，渠道落库慢于它照样
撞上、快于它白跳一轮），且整轮跳过粒度过粗。留一个方向可能错的接线点比没有更误导。

⚠️ 将来若要做，**简单维护一个全局内存时刻即可**，不要再搭注入 + 窄回调那套结构。

### 修复清单

| 问题 | 修法 |
|---|---|
| 写队列溢出淘汰丢掉**最新值** | `set` 前先 `delete`，重投递的键移到队尾 |
| `scanFetcher` 不识别 HTTP 401 | 补齐状态码阶梯，与 `ctripReadbackFetcher` 对齐 |
| `idleMs` 无下限，下发 0/负/NaN 变紧循环 | 钳到 `MIN_IDLE_MS = 30_000` |
| `byHotel` 跨渠道键冲突 | 键改 `<channel>:<otaHotelId>` |
| 比对区间从返回数据反推 min/max | 调度器透传 `windowDays`，按请求窗口算 |
| `rateCodeID` 靠索引签名混入 | 声明到 `CtripRoomRef` 上 |

⚠️ **写队列那条是 Map 语义坑**：对已存在的 key 做 `set` 会保持原插入位置，于是刚被
重新投递的格子带着最新值排在队首，被当成「最早的」丢掉。回归用例已验证能捕获旧实现。

### 新增：扫描节奏按环境分档

```
dev           60s  + [0,12s)     ← 真机测试不必再手改 idleMs
pre / online  300s + [0,60s)
```

抖动在所有环境都保留（按同样 20% 比例缩放）：去掉等于让 dev 跑与线上不同的调度形状。

> ⚠️ **此分档已于当晚取消**（`21bb119`）—— 实测 dev 用 5 分钟也够，两档取同值后
> 那个三元判断只会让人以为存在差异。现所有环境统一 `300s + [0,60s)`。
> 抖动保留的理由不变。

---

## 2026-09-21 晚｜真机验证 + 配置收敛（5 次提交）

⭐ **9.2 / 9.3 / 9.8 真机通过** —— 9.3 是本能力的立论场景，此前一直未验。

### 验证到的事实

| 项 | 结果 |
|---|---|
| 不开标签页是否扫描 | ✅ 4 个目标全命中，`source_of_truth='scan'` 1575 格 |
| 扫描窗口 | ✅ 严格 15 天（09-21→10-05），一天不多 |
| 一轮耗时 | 4 个目标串行约 **4 秒** |
| 轮间隔 | 5 分 31 秒 / 5 分 6 秒（`idleMs` + 抖动），符合配置 |
| ⭐ web 改房量 → 下一轮报差异 | ✅ 云享三人间 `limitRemain` 5→7，20:07:43 扫到 |
| ⭐ web 关房/开房 → 下一轮报差异 | ✅ 09-24 `invSwitch` 1→0→1，20:47:58 扫到 |

**扫描目标的取数来源（查库确认）**：携程取 `credentialExtra.masterHotelId`，
美团取 `credentialExtra.pois[].poiId` —— **都不查 `ota_hotel`**。库里 3 条
`ota_hotel` 全是抖音绑定，与扫描无关。抖音未注册 `inventoryScan`，零数据。

### 提交

| commit | 内容 |
|---|---|
| `8202ebf` | app-config 收敛回读配置，超时提到顶层共用 |
| `ff75060` | 快照清理改为异步分批 + 定时，不再阻塞启动 |
| `21bb119` | 扫描节奏统一 5 分钟，取消 dev 分档 |
| `09c1e05` | 扫描比对日志改打 `compared`，不再打基线总数 |
| `73aae3e` | 携程扫描与回读不再过滤预售房型 |

### ⚠️ 一个被证伪的「问题」（记下来免得再查一遍）

日志里 `cells: 360` 与 `baseline: 384` 长期对不上，一度被当成漏扫的信号。**不是问题**：

```
差的 24 格 = 自然读写入的钟点房商品（-3小时- / -4小时-）
             扫描侧按 roomCategory 滤掉，自然读照页面返回全收
```

比对以 `latest` 为主循环，基线里多出来的格子**不参与任何判断**
（`snapshot-diff.ts` 的「不做删除判定」），所以这个差值从来不是信号。

→ 日志已改成打 `compared`（本轮读到的格子里有多少在基线里找到对照），
与 `cells` / `changed` 三个数字自洽，不需要与库里总数对照（`09c1e05`）。

### 预售过滤已去掉（`73aae3e`）

扫描与回读**两侧同步**去掉 `advanceSale` 过滤，钟点房（`hourRoom`）过滤不变。

回读侧的后果更直接：`wanted` 已把范围限定成「用户本次改动的房型」，用户改的恰好是
预售房型时，过滤会把它滤空，整次回读退化成 `no-matching-room-types` 空转。

⚠️ **不能只改一边**：不一致会让预售房型被扫描建了基线、回读却永远不更新，
下一轮扫描又比对出差异，重复上报。两处都在原过滤位置留了注释（写明加回来写在哪、
必须同步另一侧），`presaleSkipped` 计数器与日志字段保留（恒为 0）。

### ⏸️ 未解释：携程价格行全是 `PP`（预付）

库里两家携程门店的价格行 `payType` **全部是 `PP`，一条现付都没有**。

已排除「代码过滤了预付」：判据只有 `hourRoom` / `advanceSale` 两个，`payType` 只是
原样带走；且 122244992 的房态行取到了**两个 `hotelID`**（`122247738` / `124241180`），
说明预付现付两套上下文都扫到了。

⚠️ 需要用户确认这两家店在携程后台**是否真有现付房型在售**，才能判断价格侧是否有缺口。

---

## 待办（用户提出，未处理）

- ⭐ **cells 组织方式（美团接入时必须先决定）**：目前房态房量一条、价格另一条
  （照携程读接口的原始结构）。美团的价格挂售卖房型（`goodsId`）、房量挂物理房型
  （`roomId`），**ID 空间不同，1:多** —— 携程两者同为 `roomTypeID` 所以没暴露这个问题。
  已写进服务端需求文档第 6 节。**这是美团接入的第一个决策点，不是收尾项。**
- ~~**扫描窗口 15 天** 与自然读实测范围对齐（已改默认值），但未真机确认取数耗时~~
  —— ✅ 已确认（2026-09-21 晚）：4 个目标串行约 4 秒，窗口严格 15 天
