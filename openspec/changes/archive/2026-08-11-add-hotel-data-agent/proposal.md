# 酒店经营数据 Agent

## 背景

酒店运营人员需要在现有 Agent 对话中直接查询真实经营数据，并沿用既有流式消息和生成式 UI 展示能力。

## 目标

- 将指定阿里云 DMS MCP 固化为 server 端酒店数据能力。
- 允许已登录员工用自然语言执行受控的只读查询。
- 对大结果、查询失败和凭证信息提供稳定的边界处理。
- 用仓库内真实 E2E 覆盖 LLM → MCP → DMS → Agent 回答链路。

## 非目标

- 本阶段不按酒店或员工细分数据权限。
- 不通过本地 RMS MySQL 查询经营数据。
- 不开放 DMS 写入、管理或审批工具。

