# Tasks

- [x] Convert the root package to a private npm workspace orchestrator.
- [x] Move Electron application source, tests, assets, and package-specific configuration into `apps/desktop`.
- [x] Normalize server workspace metadata and remove its nested lockfile from workspace ownership.
- [x] Add `packages/api` with a tested tRPC router contract.
- [x] Mount the router in SvelteKit and add the typed Electron-main client.
- [x] Generate the single root lockfile with npm.
- [x] Run workspace checks, tests, builds, Electron packaging, verification, and code review.
- [x] Synchronize the stable architecture capability spec after verification.
