# Tasks

- [x] 1. Add the workflow handler contract and registry integrity tests for workflow-ID dispatch,
      complete coverage, duplicate IDs and intent mismatches.
- [x] 2. Extract current operating-summary, generic-data, public-rate and weather collection plans
      into registered handlers without changing tool permissions or query behavior.
- [x] 3. Route evidence assessment and deterministic presentation through the resolved handler and
      remove centralized intent-specific answer probing.
- [x] 4. Update composition and focused tests, then run the affected server completion gate once.
- [x] 5. Perform independent verification and code-review passes, record evidence, and merge the
      workflow-registration delta into the stable business-execution specification.
