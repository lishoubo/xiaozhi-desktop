# Tasks

1. Update tests to require a 15-call budget for generic hotel-data workflows.
2. Add regression coverage for narrow stream-failure attribution and user-facing error mapping.
3. Forward model-driven MCP lifecycle events through the gateway observability boundary and test
   safe structured failure logging.
4. Implement the budget and failure-attribution changes with no raw MCP payload logging.
5. Run focused Agent tests, then the server completion verification suite.
6. Record verification evidence in `verification.md`.
