# Tasks

## 1. Tests and shared contract

- [x] 1.1 Add focused tests for hidden empty traces, committed-only UI and scalar Table cells.
- [x] 1.2 Replace the weather quick-action ID with the yesterday operating-review ID across the shared
  contract and catalog tests.

## 2. Server execution

- [x] 2.1 Stage generated UI until Run completion and reject nested Table cells.
- [x] 2.2 Make operating-summary collection deterministic and fail closed when its pinned SQL tool
  cannot run.
- [x] 2.3 Seed the yesterday shortcut date while preserving normal operating-summary clarification.
- [x] 2.4 Discover the exact DMS database programmatically and pin all downstream table/SQL calls.

## 3. Desktop experience

- [x] 3.1 Hide empty successful execution traces and remove the generated-UI placeholder.
- [x] 3.2 Update the new shortcut presentation and ensure final message UI remains rendered normally.
- [x] 3.3 Run the Svelte autofixer on every touched component.

## 4. Verification and governance

- [x] 4.1 Run targeted tests during implementation and one completion-state full gate.
- [x] 4.2 Perform separate verification and code-review passes with recorded evidence.
- [x] 4.3 Merge the accepted delta into the affected stable Agent capability specifications.
